services:

  mock_api:
    build:
      context: ./mock_api
    ports:
      - "4000:4000"

  runner:
    build:
      context: .
    depends_on:
      - mock_api
    environment:
      MOCK_API_URL: http://mock_api:4000
      TARGET_FPS: "5"
      DEBUG_MODE: "false"